<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/profileS">

    <ImageView
        android:layout_width="125dp"
        android:layout_height="125dp"
        android:layout_margin="2dp"
        android:scaleType="centerCrop"
        android:id="@+id/profileSavedPics"
        app:layout_constraintLeft_toLeftOf="@id/profileS"
        app:layout_constraintTop_toTopOf="@id/profileS"
        app:layout_constraintRight_toRightOf="@id/profileS"
        app:layout_constraintBottom_toBottomOf="@id/profileS"
        />

</androidx.constraintlayout.widget.ConstraintLayout>